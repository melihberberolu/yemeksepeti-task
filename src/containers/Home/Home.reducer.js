// import HOME from "./Home.constants";

const initialState = {};

const homeReducer = (state: Object = initialState, action) => {

  switch (action.type) {


    default:
      return state;
  }

};


export default homeReducer;
